class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        #no1, no2 = 0,0
        #for digit in num1:
        #    no1 = no1*10 + (ord(digit)-ord('0'))
        #for digit in num2:
        #    no2 = no2*10 + (ord(digit)-ord('0'))
        #return str(no1*no2)
        return str(int(num1)*int(num2))
        